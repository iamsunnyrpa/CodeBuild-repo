version: 0.2

phases:
  pre_build:
    commands:
      - docker build -t helloworld .
  build:
    commands:
      - docker images
      - echo "I am Sunny"
      - docker run helloworld echo "Hello, World!" 
